apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kafka-topic-monitor.fullname" . }}-test-connection"
  labels:
    {{- include "kafka-topic-monitor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kafka-topic-monitor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
