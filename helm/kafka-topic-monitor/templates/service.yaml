apiVersion: v1
kind: Service
metadata:
  name: {{ include "kafka-topic-monitor.fullname" . }}
  labels:
    {{- include "kafka-topic-monitor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kafka-topic-monitor.selectorLabels" . | nindent 4 }}
