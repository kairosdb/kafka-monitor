<?xml version="1.0" encoding="UTF-8" ?>

<metrics4j>
	<sources>
		<!-- defaults are declared at the root -->
		<collector ref="Counter"/>
		<formatter ref="template"/>
		<trigger ref="myTrigger"/>
		<sink ref="telnet"/>

	</sources>
	<sinks>
		<sink name="telnet" class="org.kairosdb.metrics4j.sinks.TelnetSink" host="localhost" port="4242" resolution="MILLISECONDS"/>
	</sinks>
	<collectors>
		<collector name="Counter" class="org.kairosdb.metrics4j.collectors.LongCounter"/>
	</collectors>
	<formatters>
		<formatter name="template" class="org.kairosdb.metrics4j.formatters.TemplateFormatter"
		           template="kafka_monitor.${methodName}"/>
	</formatters>

	<triggers> <!-- determines when to collect metrics -->
		<trigger name="myTrigger" class="org.kairosdb.metrics4j.triggers.IntervalTrigger" interval="1" unit="MINUTES"/>
	</triggers>
</metrics4j>